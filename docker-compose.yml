services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9105:5173"
      - "3011:3011"
    env_file:
      - .env
    environment:
      - APP_ENV=development
      - STORAGE_LOCAL_PATH=/app/storage/wallpapers
      - SHARED_FOLDER_PATH=/app/dev-simulated-share/mbma.com/scripts
      - PORT=3011
      - DOCKER=1
      - VITE_HMR_CLIENT_PORT=9105
    volumes:
      - ./:/app
      - app_node_modules:/app/node_modules
    command: npm run dev

volumes:
  app_node_modules:
